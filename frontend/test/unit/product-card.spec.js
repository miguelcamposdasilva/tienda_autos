import ProductCard from '../../src/components/ProductCard.jsx';

describe('ProductCard module', () => {
  it('exports a React component function', () => {
    expect(typeof ProductCard).toBe('function');
  });
});
